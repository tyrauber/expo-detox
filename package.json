{
  "name": "expo-detox",
  "private": true,
  "repository": "https://github.com/tyrauber/expo-detox.git",
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "expo": "yarn workspace @expo-detox/app"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
